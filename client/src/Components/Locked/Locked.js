import "./Locked.scss";

const Locked = (props) => {
  return (
    <div className="lockedContainer">
      To use Smishy you have to enable your camera and microphone.
    </div>
  );
};

export default Locked;
